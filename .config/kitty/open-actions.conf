# Debug: write a marker on every file:// click to verify open-actions.conf is consulted.
protocol file
action launch --type=background sh -c "echo \"$(date) URL=${URL}\" >> ~/.cache/kitty-open-action.log"
